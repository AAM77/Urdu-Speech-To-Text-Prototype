# Full local runtime dependencies for the Urdu Audio Article Prototype.
# Mirrors the `core` + `cli` + `ui` + `processor` extras in `pyproject.toml`.
# For development extras, install via:
#   pip install -e ".[dev]"
openai>=2.30,<3
pydantic>=2.13,<3
pydantic-settings>=2.13,<3
python-dotenv>=1.0,<2
streamlit>=1.55,<2
typer>=0.24,<1
rich>=14.3,<16
tiktoken>=0.10,<1
rapidfuzz>=3.10,<4
